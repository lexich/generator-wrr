"use strict";

import { connect } from "react-redux";
import <%= props.componentName %> from "./<%= props.componentName %>";

function mapStateToProps(state) {
  return {};
}

function mapDispatchToProps(dispatch) {
  return {};
}

export default connect(mapStateToProps, mapDispatchToProps)(<%= props.componentName %>);
